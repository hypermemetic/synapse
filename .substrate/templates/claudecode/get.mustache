{{! claudecode.get - returns: claudecode.GetResult }}
{{! Variants: ok, error }}
{{#ok}}
  config: claude_session_id={{config.claude_session_id}} created_at={{config.created_at}} head={{config.head}} id={{config.id}} loopback_enabled={{config.loopback_enabled}} mcp_config={{config.mcp_config}} metadata={{config.metadata}} model={{config.model}} name={{config.name}} system_prompt={{config.system_prompt}} updated_at={{config.updated_at}} working_dir={{config.working_dir}}
{{/ok}}
{{#error}}{{{message}}}{{/error}}
